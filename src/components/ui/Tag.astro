---
interface Props {
  class?: string;
}
const { class: className = '' } = Astro.props;
---
<span class={`inline-flex items-center rounded-sm bg-bg-alt px-2 py-0.5 text-meta text-ink-secondary ${className}`}>
  <slot />
</span>
